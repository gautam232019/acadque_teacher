import 'package:flutter/material.dart';

class InsideQuestionHistoryScreen extends StatelessWidget {
  const InsideQuestionHistoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(
      body: Text("lamo khau"),
    ));
  }
}
